dependencies:
  flutter:
    sdk: flutter

  # Firebase Core & Services
  firebase_core: ^3.12.0
  firebase_auth: ^5.5.1
  cloud_firestore: ^5.6.5
  firebase_storage: ^12.4.4

  # Maps & GPS Location
  google_maps_flutter: ^2.10.1
  geolocator: ^13.0.2

  # Utilities & Helpers
  provider: ^6.1.2       # Or flutter_riverpod / flutter_bloc depending on preference
  image_picker: ^1.1.2    # Ready for camera/gallery image picks
  url_launcher: ^6.3.1    # To launch Google Maps navigation externally
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0